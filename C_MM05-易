#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;

int main() {
    double n;
    cin >> n;
    double result = round(n * n * 10.0) / 10.0;
    cout << fixed << setprecision(1) << result << endl;

    return 0;
}
