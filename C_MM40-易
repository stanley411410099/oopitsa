#include <iostream>
using namespace std;
int main() {
    int N, result = 0;
    cin >> N;
    cout << "1";
    for (int i = 2; i <= N; i++) {
        cout << " + " << i;
        result += i;
    }
    cout << " = " << result+1 << endl;

    return 0;
}
