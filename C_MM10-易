#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;

int main() {
    double n;

    cin >> n;

    double fahrenheit = round((n * 9 / 5 + 32) * 10.0) / 10.0;
    cout << fixed << setprecision(1) << fahrenheit << endl;

    return 0;
}
